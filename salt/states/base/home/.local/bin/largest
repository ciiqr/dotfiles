#!/usr/bin/env bash

set -e

declare -a files=("$@")
if [[ "${#files[@]}" == 0 ]]; then
    files+=(./{,.}*)
fi

sudo du -sh "${files[@]}" 2>&1 | egrep -v '(^[0-9\.]+K|^0\s|No such file or directory)' | sort -h
