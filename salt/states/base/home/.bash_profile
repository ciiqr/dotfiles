
[[ -f ~/.shared_env ]] && . ~/.shared_env # TODO: this may not be the best place for this, if nothing else, we should have a ~/.bashenv file...
[[ -f ~/.shared_profile ]] && . ~/.shared_profile
[[ -f ~/.bashrc ]] && . ~/.bashrc
