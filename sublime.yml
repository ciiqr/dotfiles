when: os == "macos"
vars:
  sublime_config_dir: ~/Library/Application Support/Sublime Text
---
when: os == "linux"
vars:
  sublime_config_dir: ~/.config/sublime-text
---
# TODO: confirm
# when: os == "windows"
# vars:
#   sublime_config_dir: ~/AppData/Roaming/Sublime Text

---
files:
  - source: sublime/config
    destination: "{{ sublime_config_dir }}/Packages/User"
    link_files: true

# TODO: consider blocking https://stats.sublimetext.io
