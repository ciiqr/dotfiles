#!/usr/bin/env bash

set -e

declare timestamp="$1"
if [[ -n "$timestamp" ]]; then
    # pretty print timestamp
    if [[ "$OSTYPE" == darwin* ]]; then
        date -r "$timestamp"
    else
        date --date=@"$timestamp"
    fi
else
    # print current timestamp
    date "+%s"
fi
