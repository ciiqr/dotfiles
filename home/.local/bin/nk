#!/usr/bin/env bash

if [[ -d ~/Projects/nk ]]; then
    eval "$(cd ~/Projects/nk && direnv export bash 2>/dev/null)"

    exec cargo run --manifest-path ~/Projects/nk/Cargo.toml -q -- "$@"
else
    exec ~/.nk/bin/nk "$@"
fi
